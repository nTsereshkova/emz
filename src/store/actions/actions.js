import vacanciesSlice from '../slices/vacanciesSlice';
import mainSlice from '../slices/mainSlice';

export const { showDetailsHandler } = vacanciesSlice.actions;

export const { changeLanguageHandler } = mainSlice.actions;
